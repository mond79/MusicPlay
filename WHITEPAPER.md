# 📜 프로젝트 'Mond Play' 개발 백서 (Development Whitepaper)

## 1. 프로젝트 개요 (Overview)

본 프로젝트는 로컬 환경에 방치된 MP3 파일들을 심미적이고 현대적인 방식으로 감상할 수 있도록 고안된 웹 기반 커스텀 뮤직 플레이어 앱이다. 초기 목표는 단순한 데스크톱 음악 파일 재생 시스템의 구축이었으나, 사용자의 감성적 만족도와 제어 편의성을 극대화하기 위한 단계적 고도화(Evolution) 과정을 거쳤다.

수많은 기술적 허들과 브라우저 제약 사항을 극복하는 여정 속에서, 본 프로젝트는 단순한 '오디오 재생기'를 넘어섰다. 음향을 커스터마이징하는 이퀄라이저, 감상 기록을 분석하는 통계 대시보드, 그리고 시각적 몰입감을 선사하는 시네마틱 비주얼라이저와 PiP 미니 플레이어를 통합하며 비로소 **'프리미엄 로컬 음악 감상 플랫폼'**으로 완성되었다.

---

## 2. 핵심 기능 및 진화 과정 (Core Features & Evolution)

### 1차 진화: '음악 감상의 뼈대'를 세우다 (Core Framework & Streaming)
- **로컬 음원 스트리밍 기반 구축:** `Python Flask` 백엔드와 `SQLite3` 데이터베이스를 결합하여 로컬 음원 폴더를 탐색하고, Mutagen 라이브러리로 메타데이터와 앨범 아트를 정밀하게 추출.
- **Glassmorphism UI 설계:** 사용자에게 투명하고 고급스러운 시각적 경험을 제공하기 위한 최신 UI/UX 파운데이션 확립.
- **기본 플레이어 기능:** 재생목록 관리, 이전/다음 곡 제어, 프로그레스 바 탐색 및 셔플 상태 연동.

### 2차 진화: '사용자 인터랙션'의 확장 (Interaction & Context)
- **가사 파싱 엔진 (`_updateLyricsPanel`):** 타임스탬프가 기록된 LRC 규격 가사 파일을 실시간 동기화하여 노래방 스타일의 스크롤 리딩 구현. 싱크가 없는 경우 일반 텍스트 모드로 유연하게 폴백(Fallback)하는 유연성 확보.
- **스마트 라이브러리 & 컨텍스트 메뉴:** 네이티브 데스크톱 앱과 유사한 쾌적한 조작성을 제공하기 위해, 마우스 우클릭을 통해 직관적인 팝업 액션을 수행하도록 설계.

### 3차 진화: '듣는 경험'의 프리미엄화 (Premium Auditory)
- **10-밴드 오디오 이퀄라이저 (Web Audio API):** 브라우저 내장 `BiquadFilterNode`를 직렬로 엮어, 10가지(팝, 록, 힙합, R&B 등) 프리셋을 통한 전문가급 오디오 필터링 통제권 획득.
- **독립형 미니 플레이어 (Document PiP API):** 브라우저라는 공간적 제약을 산산조각 내고 최신 HTML5 기술을 도입하여, 모니터 한구석에 띄워두고 다른 작업을 할 수 있는 Spotify 스타일의 백그라운드 미니 플레이어 창 구현.

### 4차 진화: '데이터와 시각적 몰입'의 극대화 (Data & Visual Immersion)
- **내 감상 통계 대시보드 (`get_top_artists`):** 외부의 무거운 차트 라이브러리를 철저히 배제하고, 순수 CSS-Only Bar 차트 기술을 이용해 가볍고 아름다운 개인화 청취 데이터 랭킹을 시각화.
- **시네마틱 비주얼라이저 (`AnalyserNode` & `<canvas>`):** 풀스크린 전환 시, 앨범 커버를 거대한 블러(Blur) 배경으로 확장하여 극장 같은 아우라를 형성. 여기에 실시간 오디오 주파수를 파싱해 수십 개의 막대가 사운드에 맞춰 춤을 추는 오디오 스펙트럼 렌더링 완성.

### 5차 진화: '감성적 개인화와 탐색의 완성' (Emotional Personalization & Navigation)
- **스포티파이 스타일 아티스트 프로필 시스템:** 단순 정보를 넘어, 대형 히어로 배너(Hero Banner)와 다이내믹 그라데이션, 고해상도 아키텍처를 적용하여 아티스트 페이지의 미학적 수준을 스포티파이급으로 격상.
- **사용자 커스텀 아티스트 이미지 관리:** 사용자가 직접 아티스트 이미지를 업로드하고 서버에 영구적으로 기록할 수 있는 업로드 파이프라인(`Base64` 파일명 치환 기술 적용) 구축.
- **지능형 하이퍼-링크 내비게이션:** 노래 목록이나 재생 대기열 등 앱의 어디에서든 아티스트/앨범 이름을 클릭하면 상세 페이지로 즉시 순간 이동하는 심리스(Seamless) 탐색 경험 확보.

### 6차 진화: '데이터 시각화와 스캔 UX의 완성' (Data & UX Refinement)
- **비동기 백그라운드 스캔 엔진:** 스캔 중 UI 블로킹 현상을 해결하기 위해 `threading` 라이브러리로 백그라운드 처리를 구현하고, 사용자가 실시간으로 진행률을 모니터링할 수 있는 폴링(Polling) 상태 API 구축.
- **확장된 통계 대시보드 시각화:** 단순 리스트를 넘어, CSS 도넛 차트로 구현한 '장르 분포'와 실시간 재생 로그 기반의 '최근 7일 청취 활동' 막대 그래프를 도입하여 데이터 분석의 재미를 선사.

### 7차 진화: 'OS 연동과 파일 관리의 유연성' (OS Integration & Management)
- **OS 미디어 세션 연동 (Media Session API):** 브라우저라는 창을 넘어 운영체제(Windows/macOS)와 직접 통신. 키보드 물리 미디어 키(⏮⏯⏭) 지원 및 잠금 화면/알림 센터 내 곡 정보 표시로 시스템 네이티브 앱 수준의 조작감 확보.
- **개별 파일 스캔 시스템:** 폴더 전체를 스캔해야 하는 번거로움을 덜고, 사용자가 원하는 곡들만 골라 즉시 라이브러리에 추가할 수 있는 `Tkinter` 기반의 파일 탐색기 및 포인트 스캔 기술 적용.
- **PiP 미니 플레이어 UX 고도화:** 독립 창 상태에서도 모든 키보드 단축키를 동일하게 사용할 수 있도록 이벤트 전파 로직을 정비하고, 곡 전환 시 시각적 피로도를 줄여주는 페이드(Fade) 전환 애니메이션 도입.

### 8차 진화: 'LRC 가사 동기화의 시각적 완성도 극대화' (Lyric Sync Optimization)
- **GPU 가속 가사 애니메이션 최적화:** 기존 `font-size` 변경 방식의 가사 하이라이트 애니메이션이 레이아웃 Reflow를 유발하여 버벅임을 생성하는 문제를 진단. `font-size`를 `transform: scale()`과 GPU 가속 CSS 프로퍼티로 전면 교체하여 60fps의 유려한 성능을 확보.
- **시네마틱 포커스 효과 (Camera Focus):** 활성화된 현재 가사 라인은 `scale(1.0)`으로 또렷하게 표시하고, 과거/미래 라인은 `scale(0.85)` 축소와 `blur()` 필터를 혼합 적용하여 마치 카메라가 피사체에 초점을 맞추듯 몰입감 있는 가사 경험을 완성.
- **탄성 있는 전환 타이밍 함수 도입:** `cubic-bezier(0.2, 0.8, 0.2, 1)`의 수제 이징 커브를 적용하여 기계적인 선형 전환 대신 자연스럽고 스프링처럼 튀어오르는 감각적인 모션을 구현.

### 9차 진화: 'Spotify 스타일 아티스트 상세정보 패널' (Artist Bio & Credits Panel)
- **7:3 스플릿 레이아웃 도입:** 아티스트 상세 뷰의 화면을 노래 목록(좌 70%)과 상세정보 카드(우 30%)로 분리하는 CSS Flex 레이아웃을 구현. Spotify의 아티스트 상세 페이지 UX에서 영감을 받아 곡 감상과 아티스트 정보 탐색을 동시에 할 수 있는 구조를 완성.
- **나만의 아티스트 Bio 메모 (DB 연동 영구 저장):** 사용자가 직접 아티스트 소개글을 작성하고 수정할 수 있는 인라인 에디터(✏️ 토글 방식 Textarea) 구현. 작성된 Bio는 SQLite DB의 독립적인 `artists_info` 테이블에 영구 저장되어 앱 재시작 후에도 유지됨.
- **총 재생 수 실시간 자동 집계:** 해당 아티스트의 전체 곡 `play_count` 합산 값을 즉시 패널 상단에 표시하여, 사용자가 자신의 청취 이력을 직관적으로 파악할 수 있는 개인화 데이터를 제공.
- **크레딧 자동 추출 (작곡가/작사가):** 아티스트의 모든 곡에서 `composer`, `lyricist` 필드를 자동 수집 후 중복을 제거하여, Spotify 스타일로 하단 크레딧 섹션에 정갈하게 표시. 데이터가 없는 항목은 자동 숨김 처리.

### 10차 진화: '대기열 드래그 앤 드롭 & 직관적 UX 완성' (Queue Reordering & Interaction)
- **실시간 재생 대기열 순서 변경 (Drag & Drop):** HTML5 Drag and Drop API를 활용하여 재생 대기열(`Queue`) 내 곡들의 순서를 마우스로 자유롭게 조정하는 기능을 구현. 배열의 원소를 재배치하고 즉시 UI를 업데이트하는 선언적 렌더링 방식 적용.
- **시각적 드래그 피드백 시스템:** 드래그 중인 아이템의 투명도 조절, 드롭 될 위치를 나타내는 인디케이터 라인(강조선), 그리고 마우스 커서 상태 변화를 CSS로 정교하게 설계하여 조작의 확신성을 부여.
- **유연한 데이터 정렬 로직:** 곡이 이동할 때 배열 인덱스를 계산하고 보정하는 알고리즘을 통해, 어떤 위치에서든 데이터 유실 없이 순서가 완벽하게 동기화되도록 유지.

---

## 3. 주요 도전과 극복의 기록 (Key Challenges & Breakthroughs)

### 1. 막혀버린 AudioContext (안전 정책의 벽 극복)
- **문제 인식:** 최신 웹 브라우저는 사용자의 물리적인 상호작용(클릭 등) 없이는 소리 제어(AudioContext)를 시작할 수 없도록 강제 차단한다. 이로 인해 비주얼라이저와 이퀄라이저가 최초 로딩 화면에서 `Suspended` 상태로 영구 정지해버리는 치명적 먹통 현상이 발생했다.
- **해결 및 결과:** 음악을 최초 재생하는 `Player.play()` 시그널 내부에 `Equalizer.resume()` 트리거를 은밀하게 이식하고 `try-catch` 안전망을 둘렀다. 사용자의 '재생 버튼 클릭'이라는 합법적 이벤트를 활용해 맥락을 안전하게 초기화시키며 강력한 보안 샌드박스를 우회하는 데 성공했다.

### 2. 무한의 굴레, 브라우저 캐시 철벽 (Cache Busting)
- **문제 인식:** 4단계 진화 과정에서 스크립트(JS)와 스타일(CSS) 코드를 대폭 수정하고 기능을 추가했으나, Chrome 브라우저의 과도하게 강력한 캐싱 메커니즘 탓에 사용자는 계속해서 '과거의 버그난 코드'를 실행하게 되는 유령 현상에 시달렸다.
- **해결 및 결과:** `index.html`의 리소스 호출부에 버저닝 쿼리 스트링(`?v=14 → ?v=15`) 전략을 즉각 도입했다. 이를 통해 코드가 업데이트될 때마다 브라우저가 강제로 최신 파일을 내려받도록 캐시 무효화 파이프라인을 재정비하여, 업데이트 배포의 신뢰성을 되찾았다.

### 3. 두 가지 포맷을 품는 가사 엔진의 딜레마
- **문제 인식:** 시간 정보가 없는 단순 텍스트 줄바꿈(Enter) 중심의 가사 파일이, 파싱 과정을 거치며 줄바꿈이 증발하고 단일 텍스트 덩어리로 출력되어 가독성이 처참히 무너지는 버그가 발견되었다.
- **해결 및 결과:** CSS의 `white-space: pre-wrap` 속성과 `<pre>` 태그 요소 설계를 결합시켜, 브라우저가 원본 리터럴(`\n`)을 그대로 존중하고 렌더링하도록 텍스트 출력 아키텍처를 전면 개편했다.

### 4. 두 세계의 완벽한 동기화 (PiP Sync)
- **문제 인식:** 메인 창의 정보와 새로 분리된 모니터 구석의 '미니 플레이어' 간에 재생 상태, 곡 시간, 앨범 아트 변경 등 수많은 상태 정보가 불일치하며 두 개의 다른 앱처럼 겉도는 파편화 문제가 발생했다.
- **해결 및 결과:** 핵심 상태를 추적하여 전파하는 구조적 `_syncState` 동기화 로직을 도입했다. 이벤트 리스너를 활용해 본진(Main Window)과 독립된 부대(PiP Window)가 어떤 조작에도 하나의 유기적인 생명체처럼 움직이도록 완벽한 동기화 제어망을 완성했다.

### 5. 깨져버린 한글의 경고 (Encoding Defense)
- **문제 인식:** 코드 수정 자동화 과정에서 파일의 인코딩(UTF-8)이 예기치 않게 변질되어, 한글 텍스트가 파괴되고 서버가 정지되는 `UnicodeDecodeError`가 발생하여 시스템 가동이 불가능해진 상황.
- **해결 및 결과:** 소스 제어 시스템(Git)을 통한 즉각적인 롤백으로 무결성을 복구한 뒤, 모든 편집 도구가 UTF-8 규격을 엄격히 준수하도록 작업 파이프라인을 재정비함. 이를 통해 다국어 환경에서 발생할 수 있는 데이터 손실 위험을 원천 차단함.

### 6. 보이지 않는 스캔과의 싸움 (Asynchronous Scanning)
- **문제 인식:** 음악 파일이 수천 개에 달할 경우, 스캔 중 앱이 멈춘 것처럼 보이는 '응답 없음' 현상이 발생하여 사용자 경험을 해치는 문제가 발생했다.
- **해결 및 결과:** 스캔 로직을 백그라운드 스레드로 분리하고, 프론트엔드에서 500ms 간격으로 상태를 요청하는 폴링 아키텍처를 도입했다. 이제 사용자는 스캔 중에도 음악을 들으며 실시간으로 숫자가 올라가는 역동적인 재미를 느낄 수 있다.

### 7. 두 세계의 가교, 미디어 세션 (Native Control Sync)
- **문제 인식:** 브라우저가 다른 탭에 가려져 있거나 미니 플레이어가 활성화된 상태에서 키보드 단축키나 미디어 버튼이 작동하지 않아 일일이 앱을 찾아가야 하는 불편함이 있었다.
- **해결 및 결과:** `navigator.mediaSession`을 통해 OS에 재생 정보를 직접 전달하고 핸들러를 연결했다. 창의 위치와 관계없이 사용자의 물리적인 조작에 앱이 즉각 반응하도록 하여, 마치 웹 앱이 아닌 로컬 프로그램 같은 심리스한 경험을 완성했다.

### 8. 미니 플레이어의 시각적 불연속성 (State Sync & Animation)
- **문제 인식:** 곡이 바뀔 때 미니 플레이어의 앨범 아치가 뚝뚝 끊기듯이 변경되고, 폴링 방식 특유의 딜레마로 인해 약 0.5초의 정보 지연이 발생하는 미세한 불편함이 발견되었다.
- **해결 및 결과:** 메인 재생 로직에서 곡이 바뀌는 즉시 미니 플레이어에 시그널을 보내는 '수동 트리거'를 추가하고, 앨범 아트에 투명도(`opacity`) 애니메이션을 적용하여 전환의 매끄러움을 극대화했다.

---

## 4. 후반기 고도화 및 제품화 (Productization & Polish)

### 11차 진화: 'Music Play'에서 'Mond Play'로의 상용화 리브랜딩
- **브랜드 아이덴티티 확립:** 투박했던 'Music Play'라는 프로젝트명을 벗고, 모던하고 감각적인 브랜드 네임인 **'Mond Play'**로 전면 리브랜딩.
- **Typography 기반 로고 디자인:** 아이콘에 의존하지 않고 Google Fonts의 `Outfit` 글꼴과 그라데이션(`var(--accent)` 컬러)을 혼합하여, 심플하면서도 강렬한 브랜드 텍스트 로고 개발 및 적용.
- **빈 상태(Empty State) 디자인 혁신:** 처음 설치한 사용자가 빈 화면을 보고 혼란스러워하지 않도록, `당신만의 프리미엄 음악 라이브러리를 시작하세요`라는 환영 스플래시 스크린(온보딩)을 도입하여 제품 수준의 첫인상 확보.

### 12차 진화: '오디오 엔진의 진화와 편의성' (Audio Engine & Multi-Select)
- **볼륨 노멀라이제이션 (Dynamics Compressor):** 곡마다 제각각인 마스터링 볼륨 차이로 인해 사용자가 계속 볼륨을 조절해야 하는 문제를 해결. Web Audio API의 `DynamicsCompressorNode`를 이퀄라이저 체인에 직렬로 연결하여 모든 음악의 출력 볼륨을 자동 평준화(Normalization)하는 기능 탑재.
- **Soft Pause & Play (자연스러운 볼륨 페이드):** 일시정지 시 소리가 툭 끊기는 이질감을 제거하기 위해, 0.3초(300ms)에 걸쳐 `ease-out` 커브로 볼륨이 서서히 줄어들거나 커지게 만드는 극세사 타이밍 엔진 구현.
- **파워 유저를 위한 '다중 선택' 시스템:** OS 네이티브 탐색기와 동일한 경험을 제공하기 위해, `Ctrl+클릭`(개별 토글) 및 `Shift+클릭`(범위 선택) 로직을 프론트엔드에 직접 구현. 선택 시 화면 하단에서 부드럽게 솟아오르는 **플로팅 액션 바(Floating Action Bar)**와 다중 아이템 전용 우클릭 컨텍스트 메뉴를 추가해, 수십 곡을 단번에 재생/대기열/플레이리스트에 할당하는 워크플로우 완성.
- **에러 방어 및 스캔 무결성 강화:** 깨진 MP3나 확장자만 변조된 가짜 파일, 권한이 막힌 OS의 징벌적 오류 등을 만났을 때 서버가 통째로 다운되는 비극을 막기 위해 모든 추출 과정에 독립적 Try-Catch 격리소 설치. 실패한 파일은 스킵하고 온전한 파일들만 안전하게 수확하는 방탄(Bulletproof) 스캔 로직 아키텍처 수립.