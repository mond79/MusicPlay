# 📜 프로젝트 'Music Play' 개발 백서 (Development Whitepaper)

## 1. 프로젝트 개요 (Overview)

본 프로젝트는 로컬 환경에 방치된 MP3 파일들을 심미적이고 현대적인 방식으로 감상할 수 있도록 고안된 웹 기반 커스텀 뮤직 플레이어 앱이다. 초기 목표는 단순한 데스크톱 음악 파일 재생 시스템의 구축이었으나, 사용자의 감성적 만족도와 제어 편의성을 극대화하기 위한 단계적 고도화(Evolution) 과정을 거쳤다.

수많은 기술적 허들과 브라우저 제약 사항을 극복하는 여정 속에서, 본 프로젝트는 단순한 '오디오 재생기'를 넘어섰다. 음향을 커스터마이징하는 이퀄라이저, 감상 기록을 분석하는 통계 대시보드, 그리고 시각적 몰입감을 선사하는 시네마틱 비주얼라이저와 PiP 미니 플레이어를 통합하며 비로소 **'프리미엄 로컬 음악 감상 플랫폼'**으로 완성되었다.

---

## 2. 핵심 기능 및 진화 과정 (Core Features & Evolution)

### 1차 진화: '음악 감상의 뼈대'를 세우다 (Core Framework & Streaming)
- **로컬 음원 스트리밍 기반 구축:** `Python Flask` 백엔드와 `SQLite3` 데이터베이스를 결합하여 로컬 음원 폴더를 탐색하고, Mutagen 라이브러리로 메타데이터와 앨범 아트를 정밀하게 추출.
- **Glassmorphism UI 설계:** 사용자에게 투명하고 고급스러운 시각적 경험을 제공하기 위한 최신 UI/UX 파운데이션 확립.
- **기본 플레이어 기능:** 재생목록 관리, 이전/다음 곡 제어, 프로그레스 바 탐색 및 셔플 상태 연동.

### 2차 진화: '사용자 인터랙션'의 확장 (Interaction & Context)
- **가사 파싱 엔진 (`_updateLyricsPanel`):** 타임스탬프가 기록된 LRC 규격 가사 파일을 실시간 동기화하여 노래방 스타일의 스크롤 리딩 구현. 싱크가 없는 경우 일반 텍스트 모드로 유연하게 폴백(Fallback)하는 유연성 확보.
- **스마트 라이브러리 & 컨텍스트 메뉴:** 네이티브 데스크톱 앱과 유사한 쾌적한 조작성을 제공하기 위해, 마우스 우클릭을 통해 직관적인 팝업 액션을 수행하도록 설계.

### 3차 진화: '듣는 경험'의 프리미엄화 (Premium Auditory)
- **10-밴드 오디오 이퀄라이저 (Web Audio API):** 브라우저 내장 `BiquadFilterNode`를 직렬로 엮어, 10가지(팝, 록, 힙합, R&B 등) 프리셋을 통한 전문가급 오디오 필터링 통제권 획득.
- **독립형 미니 플레이어 (Document PiP API):** 브라우저라는 공간적 제약을 산산조각 내고 최신 HTML5 기술을 도입하여, 모니터 한구석에 띄워두고 다른 작업을 할 수 있는 Spotify 스타일의 백그라운드 미니 플레이어 창 구현.

### 4차 진화: '데이터와 시각적 몰입'의 극대화 (Data & Visual Immersion)
- **내 감상 통계 대시보드 (`get_top_artists`):** 외부의 무거운 차트 라이브러리를 철저히 배제하고, 순수 CSS-Only Bar 차트 기술을 이용해 가볍고 아름다운 개인화 청취 데이터 랭킹을 시각화.
- **시네마틱 비주얼라이저 (`AnalyserNode` & `<canvas>`):** 풀스크린 전환 시, 앨범 커버를 거대한 블러(Blur) 배경으로 확장하여 극장 같은 아우라를 형성. 여기에 실시간 오디오 주파수를 파싱해 수십 개의 막대가 사운드에 맞춰 춤을 추는 오디오 스펙트럼 렌더링 완성.

---

## 3. 주요 도전과 극복의 기록 (Key Challenges & Breakthroughs)

### 1. 막혀버린 AudioContext (안전 정책의 벽 극복)
- **문제 인식:** 최신 웹 브라우저는 사용자의 물리적인 상호작용(클릭 등) 없이는 소리 제어(AudioContext)를 시작할 수 없도록 강제 차단한다. 이로 인해 비주얼라이저와 이퀄라이저가 최초 로딩 화면에서 `Suspended` 상태로 영구 정지해버리는 치명적 먹통 현상이 발생했다.
- **해결 및 결과:** 음악을 최초 재생하는 `Player.play()` 시그널 내부에 `Equalizer.resume()` 트리거를 은밀하게 이식하고 `try-catch` 안전망을 둘렀다. 사용자의 '재생 버튼 클릭'이라는 합법적 이벤트를 활용해 맥락을 안전하게 초기화시키며 강력한 보안 샌드박스를 우회하는 데 성공했다.

### 2. 무한의 굴레, 브라우저 캐시 철벽 (Cache Busting)
- **문제 인식:** 4단계 진화 과정에서 스크립트(JS)와 스타일(CSS) 코드를 대폭 수정하고 기능을 추가했으나, Chrome 브라우저의 과도하게 강력한 캐싱 메커니즘 탓에 사용자는 계속해서 '과거의 버그난 코드'를 실행하게 되는 유령 현상에 시달렸다.
- **해결 및 결과:** `index.html`의 리소스 호출부에 버저닝 쿼리 스트링(`?v=14 → ?v=15`) 전략을 즉각 도입했다. 이를 통해 코드가 업데이트될 때마다 브라우저가 강제로 최신 파일을 내려받도록 캐시 무효화 파이프라인을 재정비하여, 업데이트 배포의 신뢰성을 되찾았다.

### 3. 두 가지 포맷을 품는 가사 엔진의 딜레마
- **문제 인식:** 시간 정보가 없는 단순 텍스트 줄바꿈(Enter) 중심의 가사 파일이, 파싱 과정을 거치며 줄바꿈이 증발하고 단일 텍스트 덩어리로 출력되어 가독성이 처참히 무너지는 버그가 발견되었다.
- **해결 및 결과:** CSS의 `white-space: pre-wrap` 속성과 `<pre>` 태그 요소 설계를 결합시켜, 브라우저가 원본 리터럴(`\n`)을 그대로 존중하고 렌더링하도록 텍스트 출력 아키텍처를 전면 개편했다.

### 4. 두 세계의 완벽한 동기화 (PiP Sync)
- **문제 인식:** 메인 창의 정보와 새로 분리된 모니터 구석의 '미니 플레이어' 간에 재생 상태, 곡 시간, 앨범 아트 변경 등 수많은 상태 정보가 불일치하며 두 개의 다른 앱처럼 겉도는 파편화 문제가 발생했다.
- **해결 및 결과:** 핵심 상태를 추적하여 전파하는 구조적 `_syncState` 동기화 로직을 도입했다. 이벤트 리스너를 활용해 본진(Main Window)과 독립된 부대(PiP Window)가 어떤 조작에도 하나의 유기적인 생명체처럼 움직이도록 완벽한 동기화 제어망을 완성했다.